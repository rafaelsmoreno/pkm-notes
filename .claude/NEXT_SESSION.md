# Next Session — pkm-notes

> Auto-populated by session-end hook. Do not edit manually.

## Last known state

- Branch: main (1 untracked file: `android/app/src/main/res/raw/`)
- No open PRs
- Pre-existing compile error in `src/main/frontend/components/left_sidebar.cljs` — expected, do not fix without instruction

## Pending work

- No active work item. Check KICKSTART.md if one exists, or ask the user.

## Key reminders

- Run `bb dev:lint-and-test` before any commit
- SSH not configured — use HTTPS for git operations
- ClojureScript compile fails by design (pre-existing Rum mixin error) — use `bb dev:lint-and-test` instead
